/*
 * LCD コンソールの描画周期ハンドラ。
 * SIO / target_fput_log からキューに入った文字を画面へ流し、行送りでスクロールする。
 */
#include "board_lcd.h"

CRE_CYC(LCD_CONS_CYC, { TA_STA, { TNFY_HANDLER, 0, board_lcd_console_poll }, 20000, 0 });
